
networks:
  gift.net:
    driver: bridge

services:
  app:
    build:
      context: build
      dockerfile: 8.4-cli.Dockerfile
    ports:
      - "8080:80"
    volumes :
      - ./gift.appli/:/var/php
      - ./images/img:/var/php/public/img
    working_dir: /var/php
    networks:
      - gift.net
    depends_on:
      sql:
        condition: service_healthy
    command: sh -c "composer install && php -S 0.0.0.0:80 -t /var/php/public /var/php/public/router.php"


#############################
# mysql + adminer / can be replaced by mariadb
#############################
  sql:
    image: mariadb:latest
    env_file:
      - .database_env
    volumes:
      - voldb:/var/lib/mysql
    networks:
      gift.net:
        aliases:
          - sql.db
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  adminer:
    image: adminer
    ports:
      - "8081:8080"
    networks:
      - gift.net

volumes:
  voldb:
